// Generated by scripts/generate-runtime-config-module.mjs.
// Do not edit this file directly; edit runtime-service-config.*.yaml instead.
export const runtimeServiceConfigSources = {
  "base": "# 所有环境共享的运行时配置。\n#\n# 这里**不放任何服务端点**。原先 base 写着生产地址(authUrl:\n# accounts.svc.plus / dashboardUrl: www.svc.plus / docsServiceUrl:\n# docs.svc.plus), 而配置是 base 与环境层做 merge —— 任何环境层没有显式覆盖\n# 的键都会沿用 base 的值。sit.yaml 覆盖了 apiBaseUrl 和 dashboardUrl, 却漏了\n# authUrl 和 docsServiceUrl, 于是 SIT 的认证请求一直打在生产上, 而且不报错。\n#\n# 服务端点一律由环境层给出。缺失时下游断言会失败 —— 那是想要的行为, 比沿用\n# 一个\"看起来能用\"的生产地址安全得多。\nlogLevel: info\n",
  "dev": "# 本地开发。全部指向本机 —— 本地构建绝不应该连到任何共享环境。\napiBaseUrl: http://127.0.0.1:8080\nauthUrl: http://127.0.0.1:18081\ndashboardUrl: http://localhost:3000\ndocsServiceUrl: http://127.0.0.1:3001\nlogLevel: debug\n",
  "sit": "# SIT。authUrl 与 docsServiceUrl 原先缺失, 靠 base 兜底 —— 而 base 里写的是\n# 生产地址, 所以 SIT 一直在对生产认证服务发请求。现在显式给出。\napiBaseUrl: http://127.0.0.1:8080\nauthUrl: http://127.0.0.1:18081\ndashboardUrl: http://localhost:3000\ndocsServiceUrl: https://docs.svc.plus\nlogLevel: debug\n",
  "uat": "# UAT。服务入口以 GitOps serverless runtime topology 为唯一来源:\n# console-serverless-uat / accounts-serverless-uat + TARGET_DOMAIN_BASE(onwalk.net)。\n#\n# 这个文件原先不存在, 而 loader 的 source 选择是\n# `environment === 'prod' ? 'prod' : 'sit'` —— UAT 会静默使用 SIT 的端点\n# (127.0.0.1:8080, 在 UAT 主机上并不存在), 同时通过 base 的漏项连着生产认证。\napiBaseUrl: https://accounts-serverless-uat.onwalk.net/api\nauthUrl: https://accounts-serverless-uat.onwalk.net\ndashboardUrl: https://console-serverless-uat.onwalk.net\ndocsServiceUrl: https://docs.svc.plus\nlogLevel: info\n",
  "prod": "# 生产环境。\n#\n# 顶层必须给全端点: region 的默认值是 'default', 而下面 regions 只声明了 cn\n# 与 global —— region=default 时 regions 覆盖不命中。原先 prod 顶层没有端点,\n# 靠 base 兜底才能工作(base 里写的正是这几个生产地址), 而同一个兜底让所有\n# 非生产环境也连上了生产。现在 base 不再放端点, 每个环境自己声明完整。\napiBaseUrl: https://rag-server-svc-plus-266500572462.asia-northeast1.run.app\nauthUrl: https://accounts.svc.plus\ndashboardUrl: https://www.svc.plus\ndocsServiceUrl: https://docs.svc.plus\nlogLevel: warn\nregions:\n  cn:\n    apiBaseUrl: https://rag-server-svc-plus-266500572462.asia-northeast1.run.app\n    authUrl: https://accounts.svc.plus\n    dashboardUrl: https://www.svc.plus\n    docsServiceUrl: https://docs.svc.plus\n  global:\n    apiBaseUrl: https://rag-server-svc-plus-266500572462.asia-northeast1.run.app\n    authUrl: https://accounts.svc.plus\n    dashboardUrl: https://www.svc.plus\n    docsServiceUrl: https://docs.svc.plus\n"
} as const
