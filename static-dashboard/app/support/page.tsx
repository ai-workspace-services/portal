// The support center used to be forked here because the app route was
// force-dynamic and read the request's language. It is prerenderable now, so
// this is the real page rather than a second copy that can drift.
export { default } from "../../../src/app/support/page"
